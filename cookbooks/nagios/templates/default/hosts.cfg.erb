# Generated by Chef
# host definitions

<% @nodes.each do |n| -%>
define host {
  use server
  address <%= n['ipaddress'] %>
  host_name <%= "#{n.name}" %>
  <%= "hostgroups #{n.run_list.roles.to_a.join(",")}" if n.run_list.roles.any? %>
}

<% end -%>
